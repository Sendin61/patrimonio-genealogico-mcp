[project]
name = "rob-genealogia"
version = "1.0.0"
description = "Metabuscador genealógico español con conectores MCP"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27,<1",
    "mcp[cli]>=1.2,<2",
    "starlette>=0.40,<1",
    "uvicorn>=0.30,<1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8,<9",
]
remote-ai = [
    "openai>=1,<3",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
