services:
  - type: web
    name: patrimonio-genealogico-mcp
    runtime: docker
    plan: free
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: EUROPEANA_API_KEY
        sync: false
