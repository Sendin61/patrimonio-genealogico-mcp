services:
  - type: web
    name: patrimonio-genealogico-mcp
    runtime: docker
    plan: free
    autoDeploy: true
    envVars:
      - key: ROB_ACTION_KEY
        sync: false
      - key: DATABASE_URL
        sync: false
      - key: EUROPEANA_API_KEY
        sync: false
